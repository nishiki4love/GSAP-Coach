## 工作准则

- 偏向于使用 `bun` 或 `pnpm` 作为包管理器。
- 偏向于使用 `React 19+` 生成前端代码。
- 偏向于使用 `Vite 8+` 作为主要打包工具。
- 偏向于使用 `Tailwind CSS 4+` 作为样式工具。

## 语言要求

- 一直使用简体中文进行回答。

## 输出要求

- 输出代码时需要增加清晰的中文注释，使用 JSDoc 格式。

## 编码前先思考

- 不要假设，不要隐藏困惑，主动说明权衡。
- 实现前明确说明假设；如果不确定，先询问。
- 如果存在多种理解方式，把它们列出来，不要默默选择其中一种。
- 如果有更简单的方案，主动说明；必要时可以提出反对意见。
- 如果需求不清楚，先停下来，指出不清楚的位置并提问确认。

## 简洁优先

- 用最少的代码解决问题，不做 speculative 设计。
- 不添加用户没有要求的功能。
- 不为只使用一次的代码做抽象。
- 不添加没有被要求的灵活性或可配置性。
- 不为不可能出现的场景编写错误处理。
- 如果实现明显可以更短、更直接，应重写并简化。

## 精准修改

- 只改必须修改的内容，只清理自己引入的问题。
- 不顺手优化相邻代码、注释或格式。
- 不重构没有坏掉的代码。
- 遵循项目现有风格。
- 如果发现无关的无用代码，可以提出来，但不要擅自删除。
- 删除因本次改动而变得未使用的 import、变量、函数等。
- 不删除本来就存在的无用代码，除非用户明确要求。

## 目标驱动执行

- 把任务转化成可验证的目标。
- 修复 bug 时，优先写出能复现问题的测试，再让测试通过。
- 添加校验时，优先为非法输入编写测试，再让测试通过。
- 重构时，确保重构前后测试都通过。
- 多步骤任务先给出简短计划，并标明每步的验证方式。
